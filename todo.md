Бери и делай задачи по порядку. Каждое сообщение - отдельная задача. 
После каждой задачи проверяй make check.
отмечай статус работы над задачей в этом файле ([done], [in progress], etc)

[done] в словарных наборах все еще есть проблема с частями речи - даже если у карточки есть adjective и он выбран в наборе - может отображаться карточка глагола или существительного. Проблема конкретно на странице словарного набора в отображении слов и в словарной карточке, которая открывается в рамках словарного набора. То, что для тренировки добавляются все существующие тренировочные карточки слова - ок

Исправлено:
- В GetWordSetWords добавлено case-insensitive сравнение POS в SQL запросе (LOWER(COALESCE(tc_pref.pos, '')) = LOWER(?)) - теперь правильно находит карточки независимо от регистра
- В handleLearningWordsSetStudy добавлено case-insensitive сравнение POS в Go коде (strings.ToLower) - теперь правильно выбирает карточку с нужной частью речи
- Оставлен LEFT JOIN и fallback на первую карточку, если нет matching card - показываем слово как есть