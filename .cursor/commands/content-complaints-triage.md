# Content complaints triage

Запусти `.cursor/skills/content-complaints-triage/SKILL.md`.

По умолчанию собери полный dry-run EN+ES. При «исправляй» / `apply` выполни исправления и проверки, веди журнал. Не пропускай reading_text: получение без серверного course, локальное определение курса, отдельный кластер на каждый text_id. Неизвестный курс и legacy API явно отмечай как требующие разбора. Закрывай только проверенные на production ID после релиза/импорта.
