# Copy to secrets/complaints-prod.env (gitignored) for Cursor skill / make complaints-fetch-*.
# Tokens совпадают с tts-worker/config.json → k8s TTS_INTERNAL_TOKENS_JSON (ключи en/es).
COMPLAINTS_SERVICE_URL_EN=https://linglow.qantrix.ru
COMPLAINTS_SERVICE_URL_ES=https://linglow.qantrix.ru
COMPLAINTS_SERVICE_TOKEN_EN=
COMPLAINTS_SERVICE_TOKEN_ES=
COMPLAINTS_SERVICE_TOKEN=

# Или: jq -r '.languages[] | select(.id=="en") | .api_token' ../tts-worker/config.json
# kubectl -n english get secret english-secrets -o jsonpath='{.data.TTS_INTERNAL_TOKENS_JSON}' | base64 -d
