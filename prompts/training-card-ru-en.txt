Generate ONE training card from ONE {{target_lang}} word (pair {{pair}}: {{native_lang}} → {{target_lang}}).

OUTPUT (STRICT)
- Output ONLY valid JSON, nothing else.
- Always output keys: word_target, lemma, transcription, senses, error — use only the names from SCHEMA below (neutral keys).
- If accepted: error="".
- If rejected: senses=[] and error is non-empty.

REJECTION
- Hard reject list (any casing): "microsoft", "prado", "jonas".
- Reject nonsense: if the input contains ANY digits (0-9) => reject.
- Otherwise ACCEPT (do NOT reject for uncertainty). Always ACCEPT "tor" and "bank".

SCHEMA (MUST MATCH)
{
 "word_target":"", "lemma":"", "transcription":"",
 "senses":[{"pos":"","display_word":"","word_native":"","meaning_target":"","example_target":"","example_native":"","distractors_ru":["","",""],"distractors_target":["","",""],"hint":""}],
 "error":""
}

CARD
- 1–3 senses.
- display_word: verb => "to <lemma>", else => "<lemma>".
- meaning_target MUST be in English.
- meaning_target MUST be Latin-only: it must contain Latin letters and MUST NOT contain any Cyrillic characters.
- example_target MUST be in English.
- word_native must match pos:
  - verb => Russian infinitive
  - adjective => masculine singular adjective
  - adverb => adverb
  - noun => noun (not a verb/adjective form)
  - Example: for "nod" with pos=="verb" => word_native MUST be «кивать» (NOT «кивок»).

DISTRACTORS (THIS IS THE MOST IMPORTANT PART)
- Exactly 3 RU + 3 target-language (English) items in distractors_target.
- RU: Cyrillic only. Target: English only.
- RU and target distractors are INDEPENDENT:
  - Do NOT translate distractors_target into Russian.
  - Do NOT translate distractors_ru into English.
  - Do NOT create “paired” lists by index (ru[i] is NOT a translation of en[i]).
- POS rules:
  - pos=="verb": target items are exactly "to <verb>"; RU items are infinitives.
  - pos!="verb": target items must NOT start with "to ".
- NO LEAKAGE (case-insensitive substring checks):
  - distractors_target MUST NOT contain lemma anywhere (forbid "outburst" when lemma="burst"; forbid any phrase containing "bank" when lemma="bank").
  - distractors_ru MUST NOT contain word_native anywhere (forbid "учительница" when word_native="учитель"; forbid "банкрот/банкир/банка" when word_native="банк"; forbid "поранить" when word_native="ранить").
  - Also: distractors_ru MUST NOT equal word_native exactly. NEVER include the correct word_native as a distractor (0 tolerance).
- Uniqueness: no duplicates in each list.
- STRICT FALLBACK MODE (use this to avoid validation failures):
  - If word_native is ONE word and length <= 7: you MUST use the fallback set for the sense POS AS-IS.
  - For this case, do not substitute other words and do not include word_native in distractors_ru.

FINAL SELF-CHECK (MANDATORY): if any rule fails, replace distractors until ALL pass, then output JSON.
- Ensure meaning_target is Latin-only (contains Latin, contains zero Cyrillic).

Word to process:
