Generate ONE training card from ONE {{target_lang}} word (pair {{pair}}: {{native_lang}} → {{target_lang}}).

OUTPUT (STRICT)
- Output ONLY valid JSON, nothing else.
- Always output keys: word_target, lemma, transcription, senses, error — use only the names from SCHEMA below (neutral keys).
- If accepted: error="".
- If rejected: senses=[] and error is non-empty.

REJECTION
- Hard reject list (any casing): "microsoft", "prado", "jonas".
- Hard reject list matches ONLY exact whole-token equality after trim/lowercase.
  - Example: "prado" => reject.
  - Example: "parado" => ACCEPT (must not be rejected by substring overlap).
- Reject nonsense: if the input contains ANY digits (0-9) => reject.
- Otherwise ACCEPT (do NOT reject for uncertainty).
- Always ACCEPT "castro" (must not be rejected as proper noun by uncertainty).
- General acceptance rule: if token looks like a plausible Spanish lexical item (including common loans, demonyms, adjectives/nouns, regional variants), ACCEPT it.
- Never use rejected_by_policy for uncertainty, rarity, polysemy, or "possibly proper noun" guesses.
- Mandatory pre-check order:
  1) First evaluate hard reject list and digits before any lexical analysis.
  2) If input matches any reject rule, return rejected JSON immediately.
  3) Never generate senses/examples/distractors for rejected input.
  4) Even if the token is a valid Spanish word (e.g., "prado"), hard reject list has absolute priority.

If rejected by rules above, return this exact shape:
{
 "word_target":"<input>",
 "lemma":"<input>",
 "transcription":"",
 "senses":[],
 "error":"rejected_by_policy"
}
Do not generate senses for rejected inputs.

STRICT REJECT GUARD (MANDATORY):
- `error:"rejected_by_policy"` is allowed ONLY when:
  1) token exactly matches one item from hard reject list (case-insensitive), OR
  2) token contains any digit.
- In ALL other cases you MUST output accepted card:
  - `error:""`
  - `senses` with at least 1 item.
- If you produced `rejected_by_policy`, re-check conditions above before final output; if conditions are not met, rewrite as accepted card.

ACCEPTED CARD CONTRACT (MANDATORY):
- If `senses` is non-empty, then `error` MUST be exactly empty string `""`.
- For each sense, `pos` MUST be non-empty and one of:
  `noun`, `verb`, `adjective`, `adverb`, `pronoun`, `preposition`, `conjunction`, `interjection`, `article`.
- Never output empty `pos`.
- For each sense, `word_native` MUST be non-empty Russian word/phrase in Cyrillic.
- Short tokens (2-3 Latin letters) and common international loanwords/technical terms are NOT grounds for policy rejection.
- If token is alphabetic (Spanish letters only, no digits) and length >= 4, you MUST ACCEPT it (unless it is in hard reject list).

SCHEMA (MUST MATCH)
{
 "word_target":"", "lemma":"", "transcription":"",
 "senses":[{"pos":"","display_word":"","word_native":"","meaning_target":"","example_target":"","example_native":"","distractors_ru":["","",""],"distractors_target":["","",""],"hint":""}],
 "error":""
}

CARD
- 1–3 senses.
- word_target, lemma and every display_word MUST represent exactly ONE lexical item (no comma-separated variants like "conjunto, conjunta").
- display_word: Spanish verb infinitive form as appropriate; use clear Spanish display conventions.
- transcription is REQUIRED for accepted card and MUST be non-empty (IPA-style preferred).
- meaning_target MUST be in Spanish (not in English, not in Russian/Cyrillic).
- example_target MUST be in Spanish (not in English, not in Russian/Cyrillic).
- word_native must match pos:
  - verb => Russian infinitive
  - adjective => masculine singular adjective
  - adverb => adverb
  - noun => noun (not a verb/adjective form)

DISTRACTORS (THIS IS THE MOST IMPORTANT PART)
- Exactly 3 Russian + 3 Spanish items in distractors_target.
- distractors_ru: ONLY real Russian words in Cyrillic (letters А-Яа-яЁё, spaces, hyphen only). Never output Latin transliteration or Spanish words here.
- distractors_target: Spanish (Latin script, Spanish letters only), no Cyrillic.
- The two lists are INDEPENDENT (no index pairing).
- For verb senses, distractors_ru must be Russian infinitives in Cyrillic only (forbidden examples in RU list: "describir", "imaginar", "proteger").
- NO LEAKAGE: distractors_target MUST NOT give away lemma; distractors_ru MUST NOT contain word_native as substring or exact duplicate.
- Uniqueness: no duplicates in each list.
- HARD PER-SENSE CONSTRAINTS (zero tolerance):
  - For EACH sense and EACH item in distractors_ru:
    1) must contain NO Latin letters at all;
    2) must NOT be exactly equal to word_native (case-insensitive);
    3) must NOT contain word_native as substring.
  - For EACH sense and EACH item in distractors_target:
    1) must contain NO Cyrillic;
    2) must NOT be equal to lemma and must NOT contain lemma as substring.
  - For EACH sense:
    1) meaning_target MUST contain NO Cyrillic;
    2) example_target MUST contain NO Cyrillic.
- If any of the checks above fails for a sense, REWRITE distractors for that sense before output.
- Safe fallback (use only if needed to satisfy checks):
  - verbs: distractors_ru = ["думать","читать","строить"], distractors_target = ["pensar","leer","construir"]
  - nouns: distractors_ru = ["город","страна","вопрос"], distractors_target = ["ciudad","país","pregunta"]
  - adjectives/adverbs: distractors_ru = ["быстрый","медленный","редко"], distractors_target = ["rápido","lento","raramente"]

FINAL SELF-CHECK (MANDATORY): if any rule fails, fix fields until ALL pass, then output JSON.

Word to process:
