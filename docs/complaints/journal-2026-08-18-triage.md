# Журнал триажа content reports

**Прогон:** 2026-08-18 (prod EN `qantrix.ru`, ES `es.qantrix.ru`)
**Run ID:** `triage-2026-08-18`
**Снимок:** `logs/complaints/snapshot-en-*.json`, `snapshot-es-*.json` (локально, не в git)
**Тег / коммит:** ожидает релиза
**Resolve на prod:** не выполнен; ES #14 и reading report закрыть после импорта обновлённого контента

Формат блока: **дата жалобы** → **суть** → **что изменено**.

---

## English (grammar_training | word_training)

Активных жалоб нет.

## Spanish (grammar_training | word_training)

### #14 — 2026-08-11

**Жалоба:** «Чегооо?» на вопросе `es.grammar.orientation_alphabet_sounds.stress_rules_written_accents::b1_theory_stressed_syllable::q14`. В вопросе не было правильного ответа: `silla`, `casa` и `calle` имеют ударение на предпоследнем слоге, а `café` пишется с тильдой.

**Изменено:** вариант `silla` заменён на однозначно правильный `papel`, объяснение полностью исправлено. В том же блоке уточнены два объяснения правила для согласных и исправлено перевёрнутое правило ударения в объяснении `café`.

### Reading — 2026-08-18 — `free_es_a1_ayuda_en_la_tienda_1782925007`

**Жалоба:** в учебный текст попала служебная фраза «Así practico palabras importantes del tema en un texto simple». Сам материал является диалогом покупателя и продавца, но все реплики были размечены как `narrator` и отображались прозой.

**Изменено:** служебные фразы удалены из всех 48 затронутых A1-текстов; ещё три шаблонных фразы «La respuesta final ayuda a entender el siguiente paso» удалены вместе с ними. В 13 обнаруженных диалогах реплики размечены как `speaker_a` / `speaker_b`; для 81 реплики заново сгенерировано аудио с отдельными женским и мужским голосами. В проверку reading-каталога добавлен запрет на попадание таких генераторных фраз в пользовательский текст.

---

## Правила / pattern sweep

Проверен весь испанский курс по формулировкам о базовом ударении после гласных и согласных. Других перевёрнутых правил в актуальных исходниках не найдено; совпадения в `training_pack/runs` являются историческими сырыми ответами генератора и в релиз не входят.

По reading-каталогу выполнен полный поиск обеих служебных фраз и narrator-only диалогов. Исправлены все найденные совпадения; валидатор теперь блокирует повторное попадание известных метаформулировок.

---

## Артефакты прогона

| Файл | Назначение |
|------|------------|
| `docs/complaints/journal-2026-08-18-triage.md` | этот журнал (в git) |
| `logs/complaints/triage-YYYY-MM.jsonl` | машинный журнал (локально) |
| `logs/complaints/snapshot-*.json` | снимок жалоб с prod |
