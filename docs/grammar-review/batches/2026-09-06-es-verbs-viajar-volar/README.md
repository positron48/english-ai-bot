# ES verb forms: `viajar`–`volar`

Дата: 2026-09-06.

## Объём и результат

Параллельно вычитаны и переписаны четыре банка по 96 карточек:

- `viajar` — report `4b60d7ff02e794ca67a33f53`;
- `visitar` — report `0ba68c193683a8314d608b36`;
- `vivir` — report `219220aa6ca099996f1be1c6`;
- `volar` — report `69b32301ca6b70c4b7025119`.

Итого: 384/384 карточки имеют `decision: fixed`, независимо проверены и закрыты
в `phase: done`. Редакторы и независимые проверяющие в каждом report различаются.

До правок в партии было 75 карточек с неверной `surface_form`, 128 с
повторяющимися вариантами, 165 с неверным количеством правильных ответов, 244
карточки с вариантами из другого scope, 115 карточек с вариантами вне объединения
форм леммы, 6 карточек с неверными mood/tense и 12 карточек с нарушенным blank.
После правок все эти счётчики равны нулю.

## Редактура и независимая проверка

Три сабагента и координатор редактировали непересекающиеся леммы. После полного
координаторского чтения 35 формулировок были уточнены до общего editor barrier.
Затем участники обменялись леммами и снова прочитали все 384 карточки с
подставленными формами, переводами и вариантами.

Перекрёстная проверка вернула шесть карточек исходным редакторам: две в
`visitar`, одну в `vivir` и три в `volar`. После точных исправлений те же
проверяющие повторно приняли финальные версии. `viajar` прошёл без замечаний.

Все формы сверены с локальной таблицей Jehle. Pretérito anterior и оба futuros
de subjuntivo явно помечены как книжные или юридические. Все конструкции
`De haberlo sabido` имеют явные русские контрфактические условия, а futuro
perfecto de subjuntivo передаёт завершённое действие.

Source-файлы побайтно совпадают с `internal/grammartrainingpack`. Команда
`make training-pack` не запускалась.

## Контрольные точки

В `checkpoints.jsonl` сведены 24 основные редакторские точки: по шесть порций из
16 карточек на лемму. Персональные журналы дополнительно сохраняют уточнения
координатора и исправления замечаний независимой проверки.

## Проверки

- Полная независимая проверка каждой из 384 карточек — PASS; все reports имеют
  разных `editor` и `verifier`, 384 `verification: ok`, phase `done`.
- `python3 docs/grammar-review/batches/2026-09-06-es-verbs-viajar-volar/check.py`
  — PASS: 384 формы Jehle, контракты, fingerprints, embedded-копии, варианты и
  24 checkpoints.
- Строгий `validate_artifact()` генератора — PASS для всех четырёх лемм.
- Точечный `grammar-review.py check --source` — `done` для всех четырёх лемм.
- Сохранность 25698 файлов вне разрешённого набора — 0 изменённых, 0 пропавших,
  0 неожиданных новых файлов.
- `quick_validate.py` для навыка `linglow-grammar-review` — PASS.

Финальные fingerprints:

- `viajar`: `5640fe83a237bd5c297a48f5cf228d6d8f4759dec807b43fb8b5abef1fa58460`;
- `visitar`: `2dd9e25de2cd0375dd03fa40d444290a99928a63a740701771886183ed6a3d99`;
- `vivir`: `ab450da0fcf5c628d0a6e3e25a25a4758ee7f742dd93e6f18a5313546f53ae14`;
- `volar`: `e63130c45b3436634feb5e31b4fc5c7949e0445eecb6fe2ffc1344a32f639893`.

Baseline Spanish submodule: `501044644954d5aaa474f7d1eaee99518a4ebd9d`.
Commit и push в этой партии не выполнялись.
