# ES verb forms: producir, prohibir, prometer, proponer

Дата редакторской и независимой проверки: 2026-09-06.

## Объём

- `producir`: 96/96 карточек отредактировано и независимо проверено, `done`.
- `prohibir`: 96/96 карточек отредактировано и независимо проверено, `done`.
- `prometer`: 96/96 карточек отредактировано и независимо проверено, `done`.
- `proponer`: 96/96 карточек отредактировано и независимо проверено, `done`.
- Всего: 384 карточки, 16 scopes × 6 лиц и чисел для каждой леммы.

Редактура выполнена параллельно тремя сабагентами и координатором на строго
разделённых путях. После общего барьера леммы проверили другие исполнители. Ни
один редактор не проверял собственную лемму. Общие документы и batch-проверки
изменял только координатор.

Во всех карточках вычитаны испанский контекст, русский перевод и варианты
ответа. Исходный порядок scopes, лиц и чисел и верхнеуровневые поля сохранены,
включая нестандартную позицию futuro de subjuntivo в `producir`. Source-файлы
побайтно совпадают с `internal/grammartrainingpack`; `make training-pack` не
запускался.

## Основные исправления

- Все формы сверены с локальной базой Jehle. Исправлено 79 неверных
  `surface_form`: 14 в `producir`, 19 в `prohibir`, 18 в `prometer`, 28 в
  `proponer`.
- Исправлено 78 карточек с повторяющимися вариантами: 28 в `producir`, 1 в
  `prohibir`, 47 в `prometer`, 2 в `proponer`.
- Независимая проверка выявила шесть неверно переданных контрфактических условий
  у `proponer`; «Зная это заранее» заменено на явное «Если бы … знал/знали …».
- В `prometer` после замечания координатора исправлены три причинно нелогичных
  контрфактических контекста; исправления повторно проверены.

## Контрольные точки

Каждый редактор сохранил шесть контрольных точек по 12–18 карточек. В общий
`checkpoints.jsonl` объединены 24 редакторские точки. Последующие исправления
также зафиксированы в персональных журналах.

## Проверки

- Полная независимая проверка каждой из 384 карточек — PASS; все четыре reports
  имеют разных `editor` и `verifier`, 384 `verification: ok`, phase `done`.
- `python3 docs/grammar-review/batches/2026-09-06-es-verbs-producir-proponer/check.py`
  — 384/384 карточек совпадают с Jehle, контракты и fingerprints актуальны,
  source и embedded совпадают, дубликатов prompt+answer нет.
- Строгий `validate_artifact()` генератора — PASS для всех четырёх лемм.
- `go test ./internal/verbtraining ./cmd/sync_verb_training_json` — PASS.
- `python3 scripts/grammar_review_test.py` — 13 тестов, PASS.
- `git diff --check` в корне и Spanish submodule — PASS.
- Сохранность 25403 файлов вне разрешённого набора — 0 изменённых, 0 пропавших,
  0 неожиданных новых файлов.

Baseline Spanish submodule: `501044644954d5aaa474f7d1eaee99518a4ebd9d`.
Commit и push в этой партии не выполнялись.
